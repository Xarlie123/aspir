# ASPIR — Windows CPU-only (no CUDA)
#
# Uses the PyTorch CPU wheel index. Installing this file is equivalent to:
#
#     pip install -e . --extra-index-url https://download.pytorch.org/whl/cpu
#
# The --extra-index-url line below tells pip to look at PyTorch's CPU index
# in addition to PyPI, so `torch>=2.2` resolves to a +cpu wheel.
--extra-index-url https://download.pytorch.org/whl/cpu
-e .
